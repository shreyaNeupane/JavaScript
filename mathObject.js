// Math = built-in Object that provides a
//          collection of properties and methods (mainly math related)


console.log(Math.PI);
//E represents Eulers Number
console.log(Math.E);

let x = 3.21;
let y = -2;
let z;
// to find round of number
// z=Math.round(x);
// z= Math.ceil(x); // rounds up to lowest  number
// z= Math.floor(x); // rounds up to highest number
// z = Math.trunc(x);  // to remove anything anything after decimal
// z = Math.pow(x,y); //power
// z = Math.sqrt(x);  // squareroot
// z = Math.log(x);   // log value
// z = Math.abs(y);  //gives positive value

// Trigonometric value
let a= 45;
// z = Math.sin(a);
// z = Math.cos(a);
// z= Math.tan(a);
console.log(z);

let max =Math.max(x,y,a); // to find maximum number
console.log(max);
let min = Math.min(x, y, a); // to find minimum number
console.log(min);